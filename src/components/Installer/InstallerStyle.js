import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
    installerWrapper:{
        height: "100%",
    },
    installerSidebar:{
        width: "250px",
    },
    installerContainer:{
        width: "100%",
        flex: 1,
    },
}));
